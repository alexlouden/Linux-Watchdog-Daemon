Is it better to do the memory test with real allocation?

I got one bug report that ping-mode does not work with IP_ALIASES. But since
I found no way to reproduce this I see no chance to fix it.

Call repair script with two parameter (error message + filename).

